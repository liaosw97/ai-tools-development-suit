# Plan Review — Round 2

**审查对象:** plan.md
**日期:** 2026-06-02
**前轮:** Round 1 (NEEDS_REVISION)

## Round 1 Issues 修复状态

### [major] TDD 步骤形式化
- **状态:** 部分修复
- **说明:** 批次一（Task 1.2-1.6）已包含完整的 vitest 测试代码，使用 `describe`, `it`, `expect` 进行真正的测试验证。Task 2.1 也有具体测试代码。但批次二-四的其他任务（Task 2.2-4.4）仍然只有简略描述（"添加测试验证 include 引用和差异内容保留"），没有具体测试代码。建议：要么为每个任务提供具体测试代码，要么明确说明"参考 Task 2.1 的测试模式，替换对应的 include 路径和差异内容"。

### [major] 验证命令不充分
- **状态:** 已修复
- **说明:** 验证命令现在包含内容正确性检查：
  - Task 1.2-1.6 验证特定关键词存在（如 `toContain('**触发**')`）
  - Task 1.4 还验证行数（`toBeGreaterThan(100)`）
  - Task 2.1 验证 include 引用和差异内容（如 `toContain('sdd-brainstorm')`）
  - Task 5.2-5.5 使用 diff、frontmatter 对比、路径有效性检查等

### [major] 缺少依赖关系标记
- **状态:** 已修复
- **说明:** 文档开头新增"依赖说明"部分，明确标注：
  - 批次一（Task 1.2-1.6）：可并行，无依赖
  - 批次二-四（Task 2.1-4.4）：依赖批次一完成，各任务间可并行
  - 批次五（Task 5.1-6.2）：依赖批次二-四完成
  - 每个任务都有"依赖"字段

### [minor] Task 1.1 粒度过小
- **状态:** 已修复
- **说明:** Task 1.1 已移除，原 Task 1.2 改为"创建目录并提取 `base-triggers.md`"，将创建目录和提取内容合并为一个任务。

### [minor] 缺少风险标记
- **状态:** 已修复
- **说明:** 文档开头新增"风险标记"部分：
  - ⚠️ Include 机制是纯约定（HTML 注释），无代码支持，依赖 AI 解析
  - ⚠️ 改造 SKILL.md 时需仔细保留差异内容，避免功能丢失
  - 批次二、三、四的标题也有"⚠️"标记

### [minor] Task 5.1 验证逻辑不清
- **状态:** 部分修复
- **说明:** 已添加"预期失败原因：某些测试可能因文件路径变更而失败"，但仍然不够具体。建议明确说明：(1) 哪些类型的测试会失败（路径不存在、include 语法错误等）；(2) 如何判断是预期失败还是意外失败。

### [minor] 缺少并行执行说明
- **状态:** 已修复
- **说明:** 文档开头的"依赖说明"部分明确标注了可并行的任务组，批次二、三、四的标题也注明"可并行执行"。

## 新增 Issues

### [minor] Task 5.2-6.2 的 TDD 逻辑不一致
- **说明:** Task 5.2-6.2 的 RED/GREEN 步骤更像是验证步骤而非 TDD。例如：
  - Task 5.2: RED 是"生成 diff"，验证失败是"diff 文件为空（预期，还未提交）"——这不符合 TDD 的 RED（测试失败）逻辑
  - Task 5.3: RED 是"检查 frontmatter"，验证失败是"预期输出所有 frontmatter"——同样不符合 TDD 逻辑
  - 建议：将这些任务重新定义为"验证任务"而非 TDD 任务，或者调整 RED/GREEN 逻辑使其符合 TDD 模式

### [minor] 缺少测试文件结构说明
- **说明:** 文档没有明确说明测试文件的组织方式：
  - 所有测试放在 `tests/shared-modules.test.ts` 一个文件中？还是每个任务一个测试文件？
  - Task 1.2 的测试代码显示所有测试都在 `tests/shared-modules.test.ts` 中，但后续任务的测试是追加到同一文件还是新建文件？

### [minor] Task 2.2-4.4 缺少具体测试代码
- **说明:** 这些任务的 RED 步骤只有简略描述，没有具体测试代码。虽然可以理解为"参考 Task 2.1 的模式"，但为了 TDD 的完整性，建议至少提供一个示例或明确说明"按照 Task 2.1 的测试模式，替换对应的 include 路径和差异内容"。

## Approved
- [x] 任务粒度
- [ ] TDD 步骤完整性（批次二-四的测试代码不完整）
- [x] Spec 对齐
- [x] 依赖顺序
- [x] 风险识别

## 结论
NEEDS_REVISION

**理由：**
1. 批次二-四的任务（Task 2.2-4.4）缺少具体的 vitest 测试代码，TDD 步骤不完整
2. Task 5.2-6.2 的 RED/GREEN 逻辑不符合 TDD 模式，更像是验证步骤
3. 测试文件组织方式未明确说明

**建议修复：**
1. 为 Task 2.2-4.4 添加具体测试代码，或明确说明"参考 Task 2.1 的测试模式"
2. 将 Task 5.2-6.2 重新定义为"验证任务"（去掉 RED/GREEN 标签），或调整逻辑使其符合 TDD
3. 在文档开头或 Task 1.2 中说明测试文件的组织方式
